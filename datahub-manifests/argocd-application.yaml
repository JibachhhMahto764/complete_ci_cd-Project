apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: datahub

  namespace: argocd

spec:

  project: default

  source:

    repoURL: https://github.com/YOUR_GITHUB_USERNAME/datahub-manifests.git

    targetRevision: main

    path: .

  destination:

    server: https://kubernetes.default.svc

    namespace: datahub

  syncPolicy:

    automated:

      prune: true

      selfHeal: true

    syncOptions:

      - CreateNamespace=false
